<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ericrgon.nearbox"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.ericrgon.nearbox.LoginActivity"
            android:theme="@style/LoginTheme"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity"/>
        <activity
            android:name="com.ericrgon.nearbox.LetterDetailActivity"
            android:label="@string/title_letter_detail"
            android:parentActivityName=".LetterListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".LetterListActivity" />
        </activity>
        <activity android:name=".PinActivity"/>
        <activity android:name=".PageZoomActivity"/>
        <activity android:name=".LicenseListActivity"/>
    </application>

</manifest>
